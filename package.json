{
  "name": "pi-hawk-provider",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pi extension: Hawk OAuth provider with OpenAI and Anthropic routing",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --import tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.23.1",
    "typescript": "^5.6.3"
  }
}
